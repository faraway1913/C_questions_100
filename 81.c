/*
809*？？ = 800*？？ + 9*？？,？？代表一个两位数
809*？？为四位数，8*？？的结果是两位数，9*？？的结果是三位数。
求？？代表的两位数，及809*？？后的结果
*/

#include<stdio.h>

int main(void){
    
    for(int a=10; a<100; a++){
        if(809*a>=1000 && 809*a<10000 && 8*a>=10 && 8*a<100 && 9*a>=100 && 9*a<1000){
            printf("809*%d = 800*%d + 9*%d \n",a, a, a);
            printf("result = 809 * %d = %d", a, 809*a);
        }
    }
    
    
    return 0;
}